name: azure_food_segmentation
runtime:
  name: python
  options:
    virtualenv: venv
description: MLOps implementation in azure for food segmentation
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
